(function () {
    var _ = require('lodash'),
        mixins = require('mixins');

    _.str = require('string');
    
    _.mixin(mixins);
    _.mixin(_.str.exports());
        
    exports = _;
}());